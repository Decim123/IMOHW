services:
  db:
    image: postgres:16
    container_name: web_hw_pg
    restart: unless-stopped
    network_mode: host
    env_file:
      - .env
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
