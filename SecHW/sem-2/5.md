# Task 5

Исправленная версия аудита зависимостей для `task-5`.

Каталог проекта:
- `sem-2/task-5`

Безопасные зафиксированные зависимости:
- `fastapi==0.136.0`
- `uvicorn==0.45.0`
- `pydantic[email]==2.13.3`
- `python-dotenv==1.2.2`
- `pip-audit==2.10.0`

Проверка аудита:
```bash
cd sem-2/task-5
.venv/bin/pip-audit -r requirements.txt --aliases on
```

Ожидаемый успешный результат:
```text
No known vulnerabilities found
```

Pre-commit:
- файл `.pre-commit-config.yaml` добавлен в `sem-2/task-5`
- хук запускает `pip-audit` на `requirements.txt`
