-   repo: local
    hooks:
    -   id: pip-audit
        name: pip-audit
        entry: .venv/bin/pip-audit -r requirements.txt
        language: system
        files: requirements.txt
        pass_filenames: false
